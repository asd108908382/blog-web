spring:
  application:
    name: blog-web
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://guojiawei.mysql.rds.aliyuncs.com:3306/blog?currentSchema=public&sslmode=disable&ssl=false&timezone=Asia/Shanghai
    username: root
    password: ''

  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
    show-sql: true

server:
  servlet:
    context-path: /api